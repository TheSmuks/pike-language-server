# Ralph Progress Log
Started: Wed Apr 29 11:44:51 PM CEST 2026
---

## Codebase Patterns
- WorkspaceIndex.upsertFile() builds symbol tables via buildSymbolTable(), which calls wireInheritance() internally
- wireInheritance() resolves same-file class inheritance by finding matching class declarations in the parent file scope
- Completion uses getCompletions() which takes a table + tree + CompletionContext (index, stdlibIndex, predefBuiltins, uri)
- Cross-file resolution in typeResolver uses resolveCrossFileType() which walks inherit/import declarations in the current file
- accessResolver.resolveAccessCore() resolves arrow/dot access: finds LHS declaration, calls resolveMemberAccess(), then findDeclUri()
- Tests use createTestServer() for LSP protocol tests and WorkspaceIndex directly for unit-level tests
- Corpus files are in corpus/files/ with naming convention: cross-inherit-*.pike, cross-import-*.pike
- The Declaration interface has optional sourceUri field for synthetic declarations from cross-file inheritance
- buildSymbolTable() accepts optional BuildOptions with index for cross-file inheritance wiring
---

## 2026-04-29 - US-001
Thread: N/A (Ralph agent iteration)
- Implemented cross-file inheritance lookup in wireInheritance()
- wireInheritance() now accepts optional WorkspaceIndex + URI for cross-file class resolution
- When a class-level inherit declaration can't be resolved locally, the function looks through file-level inherit/import declarations to find the target class in another indexed file
- Creates synthetic scopes in the local table that mirror remote class members (including their own inherited members)
- Added sourceUri field to Declaration for correct cross-file definition routing
- Updated accessResolver.findDeclUri() to check sourceUri for synthetic declarations
- Files changed:
  - server/src/features/symbolTable.ts (wireInheritance refactored, wireLocalInheritance + wireCrossFileInheritance added, Declaration.sourceUri added, BuildOptions interface added)
  - server/src/features/workspaceIndex.ts (upsertFile passes self to buildSymbolTable)
  - server/src/features/accessResolver.ts (findDeclUri checks sourceUri)
  - tests/lsp/crossFile.test.ts (3 new tests: cross-file wiring, chain wiring, backward compat)
  - tests/lsp/definition.test.ts (1 new test: d->speak() resolves to Animal in file A)
  - tests/lsp/hover.test.ts (1 new test: d->speak() hover shows Animal signature)
  - tests/lsp/completion.test.ts (1 new test: d-> shows Animal members via cross-file inherit)
- **Learnings for future iterations:**
  - wireInheritance runs inside buildSymbolTable, which runs inside WorkspaceIndex.upsertFile — so passing the index is straightforward
  - Synthetic declarations need sourceUri to avoid routing definition/hover to wrong file
  - The existing cross-file resolution (typeResolver.resolveCrossFileType) works at a different level — it resolves type names to declarations. The new wiring happens at scope level and benefits completion/reference resolution.
  - inheritedScopes contains scope IDs local to the table — cross-file scopes must be synthesized into the local table
  - The corpus files cross-inherit-simple-*.pike are the primary test files for cross-file inheritance
---

## 2026-04-29 - US-002
Thread: N/A (Ralph agent iteration)
- Cross-file inherited member completion — verified working after US-001 wiring
- Added deduplication test: child class overrides parent method, completion shows exactly one entry
- No code changes to server were needed — US-001's synthetic scope wiring automatically feeds into completion
- Files changed:
  - tests/lsp/completion.test.ts (1 new test: dedup when child overrides parent member)
- **Learnings for future iterations:**
  - US-001's synthetic scope approach paid off — completion, definition, and hover all work without extra wiring
  - Protected fields (e.g., Animal.name) do NOT appear in arrow access completions — this is by design
  - When testing completion dedup, use public methods not protected fields
---

## 2026-04-29 - US-003
Thread: N/A (Ralph agent iteration)
- Cross-file inherited member definition and hover — already working from US-001
- Tests were added during US-001 implementation:
  - definition.test.ts: d->speak() resolves to Animal.speak in file A
  - hover.test.ts: d->speak() hover shows Animal signature
- accessResolver.findDeclUri() now checks sourceUri on synthetic declarations
- No additional code or tests needed — acceptance criteria already met
- **Learnings for future iterations:**
  - US-001/US-002/US-003 were one design decision (synthetic scopes) decomposed into three stories
  - When a core wiring change automatically fixes downstream features, mark them together
---

## 2026-04-29 - US-004
Thread: N/A (Ralph agent iteration)
- Cross-file inherited member rename scope precision
- Added `lhsName` field to `Reference` interface for arrow/dot access refs
- Added `extractLhsIdentifier()` helper to drill through postfix_expr tree to find LHS identifier
- Added `findDeclInScopeAt()` to look up a declaration by name at a line position
- Added `isMemberOfClass()` to check if a declaration is a member (direct or inherited) of a class
- Updated `getReferencesTo()` fallback to filter arrow/dot candidates by LHS declared type
- If LHS has a declared type that resolves to a class, only include the ref if the target declaration is a member of that class
- If type resolution fails (no declaredType, type not found), fall back to name-based matching
- Files changed:
  - server/src/features/symbolTable.ts (Reference.lhsName, extractLhsIdentifier, findDeclInScopeAt, isMemberOfClass, getReferencesTo filter)
  - tests/lsp/rename.test.ts (1 new test: Dog.bark rename excludes Cat.bark)
- **Learnings for future iterations:**
  - classDecl.scopeId is the scope CONTAINING the class declaration (e.g., file scope), NOT the class body scope. The class body scope is a child scope with kind='class'. Use range matching to find it.
  - postfix_expr children: LHS is a nested postfix_expr, not an identifier. Must drill down through primary_expr → identifier_expr → identifier to get the name.
  - The Scope interface has a `kind` field ('class', 'function', 'block', etc.) that's useful for identifying scope types
---

## 2026-04-29 - US-005
Thread: N/A (Ralph agent iteration)
- Scope handlers for while, switch, and do-while blocks
- Added collectWhileStatement(), collectDoWhileStatement(), collectSwitchStatement()
- Follows the same pattern as collectIfStatement(): push scope, recurse into body, pop scope
- while_statement and do_while_statement have `body` field via childForFieldName
- switch_statement does NOT have a `body` field — the block is an unnamed child, found by type
- All three support cond_decl in condition (while, switch) for scoped condition declarations
- Added 'while', 'do_while', 'switch' to ScopeKind union
- Files changed:
  - server/src/features/symbolTable.ts (3 new handlers, 3 dispatch entries, ScopeKind extended)
  - tests/lsp/definition.test.ts (3 new tests: while/switch/do-while scope isolation)
- **Learnings for future iterations:**
  - tree-sitter-pike field names are inconsistent — while has `body`, switch does NOT. Always verify with fieldNameForChild().
  - case_clause children are siblings of local_declaration in the switch block, not nested under case_clause
  - The BLOCK_SCOPES set already listed while/switch/do-while but had no handlers — they were used for scope resolution but not for scope creation
---