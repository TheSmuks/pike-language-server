# Ralph Progress Log
Started: Wed Apr 29 11:44:51 PM CEST 2026
---

## Codebase Patterns
- WorkspaceIndex.upsertFile() builds symbol tables via buildSymbolTable(), which calls wireInheritance() internally
- wireInheritance() resolves same-file class inheritance by finding matching class declarations in the parent file scope
- Completion uses getCompletions() which takes a table + tree + CompletionContext (index, stdlibIndex, predefBuiltins, uri)
- Cross-file resolution in typeResolver uses resolveCrossFileType() which walks inherit/import declarations in the current file
- accessResolver.resolveAccessCore() resolves arrow/dot access: finds LHS declaration, calls resolveMemberAccess(), then findDeclUri()
- Tests use createTestServer() for LSP protocol tests and WorkspaceIndex directly for unit-level tests
- Corpus files are in corpus/files/ with naming convention: cross-inherit-*.pike, cross-import-*.pike
- The Declaration interface has optional sourceUri field for synthetic declarations from cross-file inheritance
- buildSymbolTable() accepts optional BuildOptions with index for cross-file inheritance wiring
- Declaration.assignedType stores initializer callee name for variables with declaredType absent or 'mixed'
- local_declaration (function-body) is NOT wrapped in declaration node; variable_decl (top-level) IS wrapped
- postfix_expr with childCount > 1 = call expression; childCount == 1 = simple reference
- declaredType || assignedType is wrong when declaredType is 'mixed'; must check !PRIMITIVE_TYPES.has(declaredType) first
- PRIMITIVE_TYPES is now centralized in symbolTable.ts (exported) and imported by typeResolver.ts, completion.ts, and server.ts
- Hover has 4 tiers: AutoDoc → Stdlib → Predef builtins → tree-sitter. Tier 4 (PikeWorker typeof) is only for variables with declaredType absent/mixed/auto
- declForHover() is synchronous; async operations (worker.typeof_) must be in the onHover handler, not inside declForHover
- Test worker interactions by monkey-patching server.server.worker.typeof_ with a stub — no Pike dependency needed

## 2026-04-29 - US-001
Thread: N/A (Ralph agent iteration)
- Implemented cross-file inheritance lookup in wireInheritance()
- wireInheritance() now accepts optional WorkspaceIndex + URI for cross-file class resolution
- When a class-level inherit declaration can't be resolved locally, the function looks through file-level inherit/import declarations to find the target class in another indexed file
- Creates synthetic scopes in the local table that mirror remote class members (including their own inherited members)
- Added sourceUri field to Declaration for correct cross-file definition routing
- Updated accessResolver.findDeclUri() to check sourceUri for synthetic declarations
- Files changed:
  - server/src/features/symbolTable.ts (wireInheritance refactored, wireLocalInheritance + wireCrossFileInheritance added, Declaration.sourceUri added, BuildOptions interface added)
  - server/src/features/workspaceIndex.ts (upsertFile passes self to buildSymbolTable)
  - server/src/features/accessResolver.ts (findDeclUri checks sourceUri)
  - tests/lsp/crossFile.test.ts (3 new tests: cross-file wiring, chain wiring, backward compat)
  - tests/lsp/definition.test.ts (1 new test: d->speak() resolves to Animal in file A)
  - tests/lsp/hover.test.ts (1 new test: d->speak() hover shows Animal signature)
  - tests/lsp/completion.test.ts (1 new test: d-> shows Animal members via cross-file inherit)
- **Learnings for future iterations:**
  - wireInheritance runs inside buildSymbolTable, which runs inside WorkspaceIndex.upsertFile — so passing the index is straightforward
  - Synthetic declarations need sourceUri to avoid routing definition/hover to wrong file
  - The existing cross-file resolution (typeResolver.resolveCrossFileType) works at a different level — it resolves type names to declarations. The new wiring happens at scope level and benefits completion/reference resolution.
  - inheritedScopes contains scope IDs local to the table — cross-file scopes must be synthesized into the local table
  - The corpus files cross-inherit-simple-*.pike are the primary test files for cross-file inheritance
---

## 2026-04-29 - US-002
Thread: N/A (Ralph agent iteration)
- Cross-file inherited member completion — verified working after US-001 wiring
- Added deduplication test: child class overrides parent method, completion shows exactly one entry
- No code changes to server were needed — US-001's synthetic scope wiring automatically feeds into completion
- Files changed:
  - tests/lsp/completion.test.ts (1 new test: dedup when child overrides parent member)
- **Learnings for future iterations:**
  - US-001's synthetic scope approach paid off — completion, definition, and hover all work without extra wiring
  - Protected fields (e.g., Animal.name) do NOT appear in arrow access completions — this is by design
  - When testing completion dedup, use public methods not protected fields
---

## 2026-04-29 - US-003
Thread: N/A (Ralph agent iteration)
- Cross-file inherited member definition and hover — already working from US-001
- Tests were added during US-001 implementation:
  - definition.test.ts: d->speak() resolves to Animal.speak in file A
  - hover.test.ts: d->speak() hover shows Animal signature
- accessResolver.findDeclUri() now checks sourceUri on synthetic declarations
- No additional code or tests needed — acceptance criteria already met
- **Learnings for future iterations:**
  - US-001/US-002/US-003 were one design decision (synthetic scopes) decomposed into three stories
  - When a core wiring change automatically fixes downstream features, mark them together
---

## 2026-04-29 - US-004
Thread: N/A (Ralph agent iteration)
- Cross-file inherited member rename scope precision
- Added `lhsName` field to `Reference` interface for arrow/dot access refs
- Added `extractLhsIdentifier()` helper to drill through postfix_expr tree to find LHS identifier
- Added `findDeclInScopeAt()` to look up a declaration by name at a line position
- Added `isMemberOfClass()` to check if a declaration is a member (direct or inherited) of a class
- Updated `getReferencesTo()` fallback to filter arrow/dot candidates by LHS declared type
- If LHS has a declared type that resolves to a class, only include the ref if the target declaration is a member of that class
- If type resolution fails (no declaredType, type not found), fall back to name-based matching
- Files changed:
  - server/src/features/symbolTable.ts (Reference.lhsName, extractLhsIdentifier, findDeclInScopeAt, isMemberOfClass, getReferencesTo filter)
  - tests/lsp/rename.test.ts (1 new test: Dog.bark rename excludes Cat.bark)
- **Learnings for future iterations:**
  - classDecl.scopeId is the scope CONTAINING the class declaration (e.g., file scope), NOT the class body scope. The class body scope is a child scope with kind='class'. Use range matching to find it.
  - postfix_expr children: LHS is a nested postfix_expr, not an identifier. Must drill down through primary_expr → identifier_expr → identifier to get the name.
  - The Scope interface has a `kind` field ('class', 'function', 'block', etc.) that's useful for identifying scope types
---

## 2026-04-29 - US-005
Thread: N/A (Ralph agent iteration)
- Scope handlers for while, switch, and do-while blocks
- Added collectWhileStatement(), collectDoWhileStatement(), collectSwitchStatement()
- Follows the same pattern as collectIfStatement(): push scope, recurse into body, pop scope
- while_statement and do_while_statement have `body` field via childForFieldName
- switch_statement does NOT have a `body` field — the block is an unnamed child, found by type
- All three support cond_decl in condition (while, switch) for scoped condition declarations
- Added 'while', 'do_while', 'switch' to ScopeKind union
- Files changed:
  - server/src/features/symbolTable.ts (3 new handlers, 3 dispatch entries, ScopeKind extended)
  - tests/lsp/definition.test.ts (3 new tests: while/switch/do-while scope isolation)
- **Learnings for future iterations:**
  - tree-sitter-pike field names are inconsistent — while has `body`, switch does NOT. Always verify with fieldNameForChild().
  - case_clause children are siblings of local_declaration in the switch block, not nested under case_clause
  - The BLOCK_SCOPES set already listed while/switch/do-while but had no handlers — they were used for scope resolution but not for scope creation
---

## 2026-04-29 - US-006
Thread: N/A (Ralph agent iteration)
- Full scope chain for local variable hover — verified working
- Added corpus file: nested-scope-chain.pike (4 levels of nesting: for > while > if)
- Scope chain walk in getDefinitionAt correctly resolves variables from outer scopes through 4+ nesting levels
- No code changes needed — existing scope chain construction is correct
- Files changed:
  - corpus/files/nested-scope-chain.pike (new corpus file)
  - tests/lsp/definition.test.ts (3 new tests: deep scope chain resolution)
- **Learnings for future iterations:**
  - getDefinitionAt works by first checking references (resolvesTo field), then declarations at position — both paths walk the scope chain correctly
  - Scope chain is constructed via parentId links during buildSymbolTable — no gaps for deeply nested constructs
  - Corpus file comment lines affect line numbering — always verify with actual parse output
---

## 2026-04-29 - US-007
Thread: N/A (Ralph agent iteration)
- Function return type tracking in type resolver
- Functions already have `declaredType` set to their return type in the symbol table
- Extended `resolveTypeMembers()` in completion.ts to handle function declarations alongside variables/parameters
- Extended `completeMemberAccess()` to extract function name from call expression LHS (e.g., makeDog() -> makeDog)
- Files changed:
  - server/src/features/completion.ts (resolveTypeMembers handles functions, completeMemberAccess extracts call name)
  - tests/lsp/completion.test.ts (2 new tests: function return type completion)
- **Learnings for future iterations:**
  - Function declarations already store their return type as `declaredType` — no new field needed
  - When the LHS of arrow access is a function call (postfix_expr ending with ()), need to extract the function name by drilling down through the tree
  - The `extractLhsIdentifier` pattern from US-004 could be reused here but the completion system uses a different code path
  - Completion positions: cursor ON the identifier (e.g., speak at col 13) triggers arrow context; cursor AFTER `->` without identifier falls through to unqualified
---

## 2026-04-30 - US-008
Thread: N/A (Ralph agent iteration)
- Assignment-based type narrowing in symbol table
- Added `assignedType?: string` field to Declaration interface
- Added `extractInitializerType()` function that drills through expression wrappers to find the innermost identifier in a variable initializer
- For variable declarations where declaredType is absent or 'mixed', records the initializer's callee name as assignedType
- Updated `resolveTypeMembers()` in completion.ts to use assignedType when declaredType is absent or a primitive
- Updated `resolveMemberAccess()` in typeResolver.ts to use assignedType similarly
- Updated rename type filtering in symbolTable.ts to also check assignedType
- Key insight: tree-sitter-pike represents `Dog()` as postfix_expr with 3 children (callee, '(', ')') — the parentheses are unnamed children. A postfix_expr with childCount > 1 indicates a call expression.
- Key insight: `declaredType || assignedType` doesn't work when declaredType is 'mixed' (truthy but useless). Must check `!PRIMITIVE_TYPES.has(declaredType)` before preferring declaredType.
- Files changed:
  - server/src/features/symbolTable.ts (assignedType field, extractInitializerType, PRIMITIVE_TYPES set, collectSimpleDecl recording, rename filter)
  - server/src/features/typeResolver.ts (resolveMemberAccess uses assignedType)
  - server/src/features/completion.ts (resolveTypeMembers uses assignedType)
  - tests/lsp/completion.test.ts (4 new tests: constructor assignment, mixed+Dog(), no initializer, complex initializer)
  - tests/lsp/definition.test.ts (1 new test: definition via assignedType)
- **Learnings for future iterations:**
  - `local_declaration` (inside function bodies) is NOT wrapped in a `declaration` node — it's a direct child of `block`. `variable_decl` (top-level) IS wrapped in `declaration`.
  - The initializer drilling in extractInitializerType must continue (not break) after extracting the callee from a call expression, because the callee itself may be wrapped in another postfix_expr.
  - PRIMITIVE_TYPES is now defined in three places: symbolTable.ts, typeResolver.ts, completion.ts. A future refactor should centralize this.
  - assignedType stores the callee name (e.g., 'Dog'), not the resolved type. The resolution happens at consumption time via resolveType().
---

## 2026-04-30 - US-009
Thread: N/A (Ralph agent iteration)
- PikeWorker typeof integration for hover on mixed/untyped variables
- Added Tier 4 hover path: when a variable's declaredType is absent, 'mixed', or 'auto', the server calls worker.typeof_() to get the real type from Pike
- Explicitly typed variables (int, string, Dog, etc.) skip the typeof call entirely
- If the worker is unavailable or times out, falls back to tree-sitter hover (no error surface)
- Exported PRIMITIVE_TYPES from symbolTable.ts; removed duplicates from typeResolver.ts and completion.ts
- The typeof integration is in the onHover handler in server.ts, NOT inside declForHover (which is synchronous)
- Added 3 tests with monkey-patched worker.typeof_ (no Pike dependency needed):
  1. Mixed variable hover calls typeof and shows inferred type
  2. Explicitly typed variable (int x) skips typeof
  3. Worker timeout falls back to tree-sitter hover
- Files changed:
  - server/src/server.ts (Tier 4 typeof integration in onHover)
  - server/src/features/symbolTable.ts (exported PRIMITIVE_TYPES)
  - server/src/features/typeResolver.ts (imported PRIMITIVE_TYPES from symbolTable)
  - server/src/features/completion.ts (imported PRIMITIVE_TYPES from symbolTable)
  - tests/lsp/hover.test.ts (3 new tests)
  - ralph/prd.json (US-009 passes: true)
  - scripts/ralph/progress.txt (Codebase Patterns updated)
- **Learnings for future iterations:**
  - typeof_() is async but declForHover is sync — integration must be in onHover, not declForHover
  - `assignedType` from US-008 can be a function name (e.g., 'makeDog'), not a class name — it's the callee identifier, not a resolved type
  - The condition for typeof is: declaredType is absent/mixed/auto. Not just 'no declaredType' — 'mixed' means the user chose not to annotate.
  - When testing worker interactions, monkey-patch worker.typeof_ with a stub function. This avoids needing Pike installed.
---

## 2026-04-30 - US-010
Thread: N/A (Ralph agent iteration)
- Type inference corpus files and harness snapshots
- Added 4 corpus files exercising type inference paths:
  - inference-return.pike: functions returning typed objects, callers accessing members
  - inference-assign.pike: variables assigned from constructors/functions, member access
  - inference-chained.pike: chained a()->b()->c() access patterns
  - inference-failure.pike: mixed returns, unknown types, unresolvable access
- Generated harness snapshots for all 4 files (63 total)
- Updated corpus/manifest.md with new Type Inference section and counts
- Files changed:
  - corpus/files/inference-*.pike (4 new files)
  - harness/snapshots/inference-*.json (4 new snapshots)
  - corpus/manifest.md (new section + updated counts)
  - ralph/prd.json (US-010 passes: true)
  - scripts/ralph/progress.txt (Codebase Patterns updated)
- **Learnings for future iterations:**
  - Harness snapshot generation: `bun run harness:snapshot` generates snapshots for ALL corpus files, not just new ones
  - inference-failure.pike has exit_code=1 (compilation errors) — this is intentional for error-case corpus files
  - Corpus manifest counts in header (e.g., '39 committed') and summary table must be kept in sync
---

## 2026-04-30 - US-011
Thread: N/A (Ralph agent iteration)
- Decision document for type inference and Phase 10 audit
- Created docs/decisions/0019-type-inference.md covering: return type tracking, assignment narrowing, typeof integration, depth limits, fallback behavior
- Created docs/decisions/0002-tier-3-scope.md with Phase 10/11 update section documenting US-008/009/010 improvements and remaining gaps
- Updated docs/known-limitations.md: marked 2 items as partially resolved, added 4 new limitations from Phase 10/11
- Files changed:
  - docs/decisions/0019-type-inference.md (new)
  - docs/decisions/0002-tier-3-scope.md (new)
  - docs/known-limitations.md (updated)
- **Learnings for future iterations:**
  - Decision documents go in docs/decisions/ with 4-digit numbered prefix
  - ADR template is at docs/decisions/0000-template.md
  - known-limitations.md uses ✅ markers for resolved items and clearly notes partial resolution
---

## 2026-04-30 - US-012
Thread: N/A (Ralph agent iteration)
- Semantic token type and modifier mapping
- Created server/src/features/semanticTokens.ts with:
  - TOKEN_TYPES array (9 types: class, enum, enumMember, function, method, variable, parameter, type, namespace)
  - TOKEN_MODIFIERS array (5 modifiers: declaration, definition, readonly, static, deprecated)
  - tokenTypeForDeclKind() function mapping DeclKind to token type index
  - tokenModifiersForDecl() computing modifier bitmask
  - SEMANTIC_TOKENS_LEGEND for capability response
- Created docs/decisions/0020-semantic-tokens.md with mapping rationale
- Key design decisions:
  - function vs method distinction: scope-based (producer checks if function is in class scope)
  - constant uses 'variable' type + 'readonly' modifier (follows TS/Python convention)
  - inherit/import use 'namespace' type (closest LSP match)
  - Legend indices are a wire contract — MUST NOT change after initialization
- Files changed:
  - server/src/features/semanticTokens.ts (new)
  - docs/decisions/0020-semantic-tokens.md (new)
- **Learnings for future iterations:**
  - Semantic token indices are wire-format — cannot reorder after client init
  - Method/function distinction requires scope context — token producer (US-013) handles this
  - SEMANTIC_TOKENS_LEGEND must be exported for server capability registration (US-014)
---

## 2026-04-30 - US-013
Thread: N/A (Ralph agent iteration)
- Semantic token production from symbol table
- Added to server/src/features/semanticTokens.ts:
  - SemanticToken interface (line, character, length, typeId, modifiers)
  - produceSemanticTokens(table) — walks declarations and references, produces sorted tokens
  - deltaEncodeTokens(tokens) — converts to LSP wire format [deltaLine, deltaChar, length, typeId, modifiers, ...]
  - isInClassScope() — checks if a declaration is inside a class scope by walking scope chain
  - resolveDeclTokenType() — promotes function→method when in class scope
- Key design decisions:
  - Only produces tokens for declarations and unresolved references
  - Resolved references are skipped (clients handle reference highlighting separately)
  - Unresolved type_ref references get 'type' token (index 7)
  - Function declarations in class scope are promoted to method (index 4)
  - Class members get 'static' modifier (bit 3)
- Added 20 tests in tests/lsp/semanticTokens.test.ts
- Files changed:
  - server/src/features/semanticTokens.ts (production + delta encoding)
  - tests/lsp/semanticTokens.test.ts (new, 20 tests)
- **Learnings for future iterations:**
  - LSP delta encoding: when line changes, character position is absolute (not relative to prev token)
  - Tokens must be sorted by (line, character) before delta encoding
  - Declaration scopeId + scopeById walk determines class scope context
  - The function→method promotion stops at function/lambda scope boundaries
---

## 2026-04-30 - US-014
Thread: N/A (Ralph agent iteration)
- Semantic token LSP handler registration
- Added semanticTokensProvider capability to initialize response with full=true
- Registered textDocument/semanticTokens/full handler that calls produceSemanticTokens + deltaEncodeTokens
- Fixed SEMANTIC_TOKENS_LEGEND type: removed 'as const' to satisfy LSP's mutable string[] type
- Added 5 LSP protocol tests (class+methods, vars+params, unknown doc, enum, inherit)
- Files changed:
  - server/src/server.ts (capability + handler registration)
  - server/src/features/semanticTokens.ts (type fix)
  - tests/lsp/semanticTokens.test.ts (5 protocol tests)
- **Learnings for future iterations:**
  - LSP type definitions use mutable types — 'as const' on exported legends causes TS errors
  - semanticTokens/full uses connection.onRequest() not a named handler like onHover
  - Handler pattern: get doc → get symbol table → produce tokens → delta encode → return { data }
---

## 2026-04-30 - US-015
Thread: N/A (Ralph agent iteration)
- Document highlight handler
- Registered textDocument/documentHighlight handler and documentHighlightProvider capability
- Uses getReferencesTo() for finding references and getDefinitionAt() for the declaration
- Declaration sites → Write (kind=3), reference sites → Read (kind=2)
- Skips references at the same position as the declaration to avoid duplicates
- Added 6 tests: variable read/write, function call, class usage, parameter, unknown doc, empty position
- Files changed:
  - server/src/server.ts (handler + capability registration)
  - tests/lsp/documentHighlight.test.ts (new, 6 tests)
- **Learnings for future iterations:**
  - DocumentHighlightKind values: Text=1, Read=2, Write=3 (NOT 0/1/2)
  - getReferencesTo() returns both the declaration reference and actual references — need to skip duplicates with getDefinitionAt()
  - Reference character positions: '  int result = add(1, 2);' → 'add' starts at column 15 (2+4+7+2)
---

## 2026-04-30 - US-016
Thread: N/A (Ralph agent iteration)
- Folding range handler
- Created server/src/features/foldingRange.ts with produceFoldingRanges()
- Foldable structures: class_body, block (functions, if/for/foreach/while/switch), comment groups
- Registered textDocument/foldingRange handler and foldingRangeProvider capability
- Comment groups: consecutive line_comment nodes spanning multiple lines get 'comment' kind
- Single-line blocks are not foldable (only spans > 1 line)
- Added 8 tests: 6 unit tests + 2 protocol tests
- Files changed:
  - server/src/features/foldingRange.ts (new)
  - server/src/server.ts (handler + capability)
  - tests/lsp/foldingRange.test.ts (new, 8 tests)
- **Learnings for future iterations:**
  - tree-sitter node.child(i) can return null — must null-check
  - Foldable ranges are determined by node types: class_body and block cover all Pike block structures
  - Comment groups need manual grouping logic — tree-sitter has no 'comment_group' node
---

## 2026-04-30 - US-017
Thread: N/A (Ralph agent iteration)
- Signature help handler
- Created server/src/features/signatureHelp.ts with:
  - produceSignatureHelp() — finds enclosing call, resolves callee, extracts parameters, tracks active param
  - findEnclosingCall() — walks up tree to find postfix_expr with parens
  - extractCalleeInfo() — extracts callee name, handles arrow/dot access (d->speak → speak)
  - countActiveParameter() — counts commas before cursor in argument list
  - countCommasInNode() — recursive comma counting inside argument_list/comma_expr
  - buildSignatureFromDecl() — extracts params from symbol table scope structure
  - buildSignatureFromStdlib() — parses params from stdlib autodoc signature string
  - splitParams() — comma-aware parameter text splitter respecting nested parens
- Registered textDocument/signatureHelp handler with trigger chars '(' and ','
- Key challenges solved:
  - Method parameters are in a child scope of the class scope — must match by range overlap
  - Arguments wrapped in argument_list → comma_expr nodes — recursive descent needed
  - Callee extraction: d->speak() → callee text is 'd->speak', must extract 'speak' after '->'
- Added 5 LSP protocol tests and decision document
- Files changed:
  - server/src/features/signatureHelp.ts (new)
  - server/src/server.ts (handler + capability)
  - tests/lsp/signatureHelp.test.ts (new, 5 tests)
  - docs/decisions/0021-signature-help.md (new)
- **Learnings for future iterations:**
  - tree-sitter-pike wraps comma-separated args in 'comma_expr' nodes, not flat list
  - 'argument_list' is a child of 'postfix_expr', containing the comma_expr
  - Class methods: the class scope contains the function declaration; the function body scope (child) has parameters. Must match by range, not just first child.
  - Stdlib autodoc uses qualified names (predef.X.Y) — simple name matching won't find them. Predef builtins use a separate index.
---

## 2026-04-30 - US-018
Thread: N/A (Ralph agent iteration)
- Code action: remove unused variable
- Created server/src/features/codeAction.ts with:
  - produceCodeActions() — produces CodeAction objects from diagnostics
  - MATCH_UNUSED_LOCAL — matches diagnostics with source 'pike' and message 'Unused local variable'
  - removeUnusedVariableLine() — produces TextEdit to delete the declaration line
  - QuickFix registry pattern for extensibility
- Registered textDocument/codeAction handler and codeActionProvider capability
- Added missing resolveAccessDefinition import (pre-existing type error fix)
- 5 tests: unused var gets action, non-matching diagnostic, other source, last line edge case
- Files changed:
  - server/src/features/codeAction.ts (new)
  - server/src/server.ts (handler, capability, import fix)
  - tests/lsp/codeAction.test.ts (new, 5 tests)
- **Learnings for future iterations:**
  - Code action receives diagnostics from the client context — the server doesn't need to store them
  - The quick-fix registry pattern (DiagnosticMatcher + EditProducer) is extensible for US-019 and beyond
  - Last-line deletion needs special handling: delete from previous line's newline to end
---

## 2026-04-30 - US-019
Thread: N/A (Ralph agent iteration)
- Code action: add missing import
- Extended server/src/features/codeAction.ts with:
  - MATCH_UNDEFINED_IDENTIFIER — matches diagnostics with source 'pike' and message 'Undefined identifier X'
  - addMissingImport() — checks if identifier matches stdlib module, inserts import statement
  - findImportInsertionLine() — finds insertion point after existing imports/inherits/#pike directives
  - Refactored QuickFix type to support dynamic titles (function instead of string)
  - Added CodeActionContext parameter with stdlibModules Set
- Added buildStdlibModules() in server.ts to extract module names from autodoc index (74 modules)
- 3 new tests: matching stdlib module, insertion after existing imports, unknown identifier no action
- Files changed:
  - server/src/features/codeAction.ts (extended)
  - server/src/server.ts (stdlibModules extraction, context passing)
  - tests/lsp/codeAction.test.ts (3 new tests, 8 total)
- **Learnings for future iterations:**
  - CodeActionContext pattern allows extending fixes without changing the handler signature
  - The import insertion point must skip comments and blank lines before the first real directive
  - Dynamic titles (function returning string) allow parameterized fix names like 'Add import Stdio'
  - The stdlibAutodocIndex keys are FQNs like 'predef.Module.Class.method' — extract Module from parts[1]
---