# Quality gate fixtures

Each subdirectory is copied into an isolated temporary repository by tests/quality-gates/detectHarness.ts.
Fixtures intentionally contain malformed or clean TypeScript/JSON examples for detector rules.
