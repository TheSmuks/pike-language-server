name: Update Pike Dependencies

on:
  schedule:
    - cron: '0 6 * * 1'  # Monday 06:00 UTC
  workflow_dispatch:
  push:
    branches: [main]
    paths: ['pike.json']

permissions:
  contents: write
  pull-requests: write

jobs:
  update-deps:
    uses: TheSmuks/pmp/.github/workflows/dep-update.yml@7b3f9f746497ba639d2bc065cd20769f15c5203a
