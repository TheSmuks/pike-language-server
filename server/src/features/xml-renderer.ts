// ---------------------------------------------------------------------------
// xml-renderer.ts: AutoDoc XML → markdown rendering
// Re-exports from sub-modules so external imports continue to work.
// ---------------------------------------------------------------------------

export { renderType, renderSignature } from './xml-renderer-types';
export { renderInline } from './xml-renderer-inline';
export { renderBlocks, renderDocGroup } from './xml-renderer-blocks';
